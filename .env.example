# Database Configuration
DATABASE_URL=postgresql://username:password@localhost:5432/youarewe
# Required by PostgreSQL. Format: postgresql://username:password@host:port/database_name

# Authentication Configuration
NEXTAUTH_URL=http://localhost:3000
# URL of your frontend application, required by NextAuth.js

NEXTAUTH_SECRET=your-nextauth-secret
# Random string used to hash tokens, sign cookies and generate cryptographic keys
# Generate using: openssl rand -base64 32

# API Configuration
NEXT_PUBLIC_API_URL=http://localhost:1337
# URL of your Strapi API

# Strapi Configuration
JWT_SECRET=your-jwt-secret
# Secret used by Strapi for JWT generation
# Generate using: openssl rand -base64 32

APP_KEYS=key1,key2
# Comma-separated list of keys used by Strapi
# Generate using: openssl rand -base64 32,openssl rand -base64 32
