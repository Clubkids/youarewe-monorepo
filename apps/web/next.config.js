/** @type {import('next').NextConfig} */
const nextConfig = {
    reactStrictMode: true,
    // Completely disable dev indicators
    devIndicators: false,
    // Keep any other existing settings in your next.config.js
  }
  
  module.exports = nextConfig;